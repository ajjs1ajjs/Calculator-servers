# Історія змін

## 2026-08-14 — Версія 2.0.1: Вільне керування модулями, чистота описів, фікс діалогів

- Усі модулі (App Server / ROBOT / Web / ForceBPM / LMS / HR) та опціональні вузли (Сервер звітів / SQL Secondary / HAProxy) вільно вмикаються/вимикаються окремо — без автоматичних блокувань за типом розгортання.
- Прибрано «Документообіг» з описів UI та README; прибрано згадки імпорту Excel.
- Виправлено діалоги пароля (шлях до тем) — кнопки «Зберегти/Перерахувати/Змінити пароль/Скинути» працюють коректно.

## 2026-08-14 — Версія 2.0.0: Ребрендинг, 3 кроки, захист матриці, повний аудит

- Перейменовано проєкт `AIResourceCalculator` → `ResourceCalculator` (namespace, проєкт, тести, інсталятор, скрипти, CI, сертифікат); видалено мертвий `AiProvider`.
- Інтерфейс тепер 3 кроки: **«Матриця» → «Параметри розрахунку» → «Результати»**; кнопка «Перерахувати» застосовує зміни без запису на диск.
- **Захист матриці паролем**: зміна діапазонів/формул/вузлів потребує пароля (SHA-256 + сіль у `settings.json`); при забутому паролі — кнопка «Перегенерувати пароль» генерує новий пароль і надсилає його на контакти розробника (email/телефон); зміна пароля через UI.
- **Редагування всіх даних без передеплою**: усі діапазони (SQL/Postgres/Oracle/App/Web), компоненти з формулами, вузли інфраструктури та константи рушія (SmartID, IOPS-профілі, ліміти SQL, pagefile) — через матрицю, збереження в `matrix.json` (SchemaVersion 10).
- Єдиний профіль навантаження «Документообіг» (Performance) — прибрано дублікати Basic-діапазонів.
- Повний аудит розрахунку, звітів (Excel/PDF), валідації та тестів; виправлено перехід на вкладку «Результати» після розрахунку.
- Тести: 134 (додано AccessService).

## 2026-07-16 — Реліз 1.1.9: розмитий шрифт після редизайну

- Стиль карток `Card` (застосований майже до всіх панелей у новому дизайні 1.1.8) мав
  `DropShadowEffect` для тіні. WPF-ефекти рендерять весь вміст елемента, включно з текстом,
  через проміжний растеризований шар — у деяких середовищах (RDP/VM/певні відеодрайвери) це
  вимикає чітке згладжування шрифтів, і текст виглядає розмитим ("у тумані").
- Прибрано `DropShadowEffect` з картки, кнопки "Розрахувати ресурси" та активного стану
  перемикача середовищ — картки лишились заокругленими, просто без тіні.
- Додано `UseLayoutRounding` та `TextOptions.TextFormattingMode="Display"` на рівні вікна —
  додаткова гарантія чіткого рендеру тексту.

## 2026-07-16 — Реліз 1.1.8: оновлений дизайн інтерфейсу

- Оновлено візуальний стиль застосунку (шапка, картки, вкладки, KPI-блок результатів,
  середовища DEV/TEST/PreProd, кнопки експорту перенесені в шапку) — суто зовнішній вигляд,
  без змін розрахунків, формул чи правил сайзингу.
- Картки середовищ DEV/TEST/PreProd тепер рівномірно заповнюють ширину рядка замість
  фіксованої ширини з порожніми полями з боків.
- На вкладці "Калькулятор" після розрахунку з'являється короткий підсумок (CPU/RAM/Диски/
  IOPS/ВМ) з посиланням на вкладку "Результати".

## 2026-07-16 — Реліз 1.1.7: реальний текст ліцензійної угоди в інсталяторі

- Інсталятор (MSI) показував на кроці "License Agreement" дефолтний текст-заглушку WiX
  (Lorem Ipsum) замість реальної угоди — WixUI_Minimal без явного WixUILicenseRtf підставляє
  свій вбудований приклад.
- Додано `ResourceCalculator.Installer/Resources/License.rtf` з текстом про те, що застосунок
  призначений виключно для внутрішнього використання співробітниками та партнерами IT-Enterprise,
  і підключено його через `WixVariable WixUILicenseRtf` у Package.wxs.

## 2026-07-16 — Реліз 1.1.6: заголовки таблиць в Excel-звіті більше не обрізаються

- Аркуші "Інфраструктура" та "ВМ по середовищах": шапка таблиці мала фіксовану висоту 26pt,
  через що довгі заголовки колонок ("Диск Logs/TempDB (ГБ)", "Диск MainData (ГБ)",
  "Диск/сервер (ГБ)") у вузьких колонках переносились у 3 рядки й обрізались
  (напр. "Диск Logs/Te" замість повного тексту).
- Висоту рядка шапки для цих двох блоків збільшено до 44pt — заголовки тепер повністю видимі.

## 2026-07-16 — Реліз 1.1.5: оформлення звіту Excel — без "н/д" та агресивного сірого

- Клітинки "не застосовно" в аркуші "Інфраструктура" тепер показують довге тире (—) замість
  тексту "н/д" — виглядає спокійніше й не читається як помилка.
- Прибрано контрастну сіру заливку таких клітинок: лишилась лише тонка світло-сіра рамка,
  щоб таблиця не "рябіла".
- Клітинка з тире вирівняна по центру, як і решта числових/кодових колонок.

## 2026-07-14 — Реліз 1.1.4: сірі "н/д" клітинки та реальні значення для Master node/Worker-node/App/Web у звіті

- Звіт "Інфраструктура" тепер явно розрізняє "не порахували" від "не застосовно за задумом":
  клітинки, які об'єктивно не стосуються ролі вузла (HAProxy — весь блок диск/IOPS; Master/Worker-node —
  розподіл дисків і файл підкачки; SQL Server — файл підкачки), рендеряться сірим курсивом "н/д"
  замість порожнього рядка.
- **Master node**: IOPS/Профіль IOPS/MiB/s/Затримка тепер заповнені за офіційним etcd sizing guide
  (etcd фізично на цьому ж вузлі, хоч і "зовнішній" відносно control plane).
- **Worker-node**: MiB/s тепер похідне значення від наявних IOPS/латентності (не порожнє).
- **Сервери додатків / Веб-сервери (IIS)**: додано тимчасові орієнтири MiB/s і Затримки
  (Microsoft IIS capacity planning) — підлягають уточненню за результатами LT-тесту.

## 2026-07-14 — Реліз 1.1.3: HAProxy знову клікабельний для DEV/TEST/PreProd у гібриді

- Чекбокси HAProxy для DEV/TEST/PreProd розблоковано — вони клікабельні незалежно від типу
  розгортання, без автоматичного примусового ввімкнення. Заблокованим і автоматичним лишається
  лише верхній прапорець PROD (обов'язковий у Гібриді для балансування між Windows-VM і K8s-частиною).

## 2026-07-14 — LMS-GraphQL скориговано за навантажувальним тестом

- Замінено формулу масштабування LMS-GraphQL з `Per25Users` (давала удвічі більше реплік, ніж
  реально знадобилось) на точки реального навантажувального тесту: 50/100/150/200/250 користувачів
  → 1/2/3/5/7 реплік, з екстраполяцією за межами 250 (+2 репліки/50 користувачів).
- CPU на репліку піднято з 0.09 до 0.25 (K8s requests з тесту), RAM — 0.3 → 0.25.
- Версію матриці піднято (v8), щоб виправлення підхопилось і в уже збережених matrix.json.

## 2026-07-14 — HAProxy некликабельний у гібриді

- Чекбокси HAProxy (PROD/TEST/PreProd) тепер заблоковані на час Гібрид-розгортання — значення
  виставляється лише автоматично, вручну не змінити. При переході на Kubernetes чи Windows
  чекбокси розблоковуються і скидаються до звичайного вимкненого стану.

## 2026-07-14 — Частота CPU, диски колонками у звіті, HAProxy в гібриді, ForceBPM

- **Частота CPU (ГГц)** — тепер показується колонкою поруч із CPU (ядер) у таблицях "ВМ по
  середовищах" та "Інфраструктура" (програма і Excel/PDF-звіти).
- **Вимоги до дисків у звіті** — прибрано окремий аркуш Excel/секцію PDF; замість цього таблиця
  "Інфраструктура" розписує диск по частинах колонками (OS/Logs-TempDB/MainData/Content).
- **HAProxy в гібриді** — вмикається за замовчуванням для PROD/TEST/PreProd (не DEV) при виборі
  Гібрид-розгортання; можна вимкнути вручну.
- **ForceBPM у гібриді** — чекбокс розблоковано (раніше завжди заблокований і в K8s, і в Гібриді);
  у чистому Kubernetes лишається заблокованим.

## 2026-07-14 — Поле «Обсяг Content, ГБ» тепер і для DEV/TEST/PreProd

- Поле обсягу Content (раніше лише PROD) додано в картки DEV/TEST/PreProd — незалежне значення
  для кожного середовища. 0 = диск Content не виділяється (типова поведінка non-prod без змін);
  явно введене число замінює це і додає диск Content у розрахунок цього середовища.

## 2026-07-14 — Вимоги до дисків для Kubernetes-вузлів, у звітах, поле Content БД

- **Вимоги до дисків**: Kubernetes-вузли (Master/Worker node) тепер показуються в панелі так само,
  як SQL Server, — раніше вузли без IOPS/pagefile/розбивки дисків (напр. Master node) мовчки
  пропускалися (`DiskAdvisor.cs`).
- Панель **«Вимоги до дисків»** тепер потрапляє і в **Excel** (окремий аркуш), і в **PDF**-звіт —
  раніше була лише в програмі.
- Нове поле **«Обсяг Content, ГБ»** (під «Обсяг даних БД, ГБ», лише PROD) — задає точний розмір
  диска Content (холодні/архівні дані), замінюючи фіксоване значення з матриці, за тим самим
  принципом, що й обсяг MainData.

## 2026-07-14 — MSI-інсталятор, версія в UI, обов'язковий бамп версії при релізі

- Додано **MSI-інсталятор** (`ResourceCalculator.Installer`, WiX v4) як альтернативу
  портативному exe — класичне оновлення Windows Installer: інсталяція новішого MSI сама знаходить
  і замінює попередню версію (незмінний `UpgradeCode`, `MajorUpgrade` блокує встановлення старішої
  версії поверх новішої).
- Версія застосунку тепер видна прямо в шапці головного вікна (поруч із підзаголовком).
- Версія переведена в єдине джерело — `AppVersion` у новому `Directory.Build.props`, яке
  успадковують і exe-проєкт, і MSI-проєкт.
- Новий `release.ps1` автоматизує весь випуск релізу (build → тести → publish → підпис → MSI →
  git tag/push → GitHub Release) і **примусово блокує реліз**, якщо тег поточної версії вже існує —
  без бампу `AppVersion` перед релізом опублікувати новий тег не вийде.

## 2026-07-14 — Сповіщення про нові версії

- При запуску застосунок у фоні звертається до GitHub Releases репозиторію й порівнює останню
  опубліковану версію з поточною. Якщо вийшла новіша — показує діалог із пропозицією відкрити
  сторінку завантаження. Мережеві помилки (немає інтернету, rate limit) проковтуються мовчки —
  це не критична перевірка. Версія в `ResourceCalculator.csproj` (`InformationalVersion`) тепер
  має бампатися при кожному релізі — інакше перевірка не побачить оновлення.

## 2026-07-13 — Прибрано «Продукт» і «Профіль навантаження» зі звіту

- Після переходу застосунку виключно на профіль Документообіг (вибір продукту прибрано з UI)
  рядки «Продукт: Документообіг» та «Профіль навантаження: Продуктивний» у заголовку PDF та на
  аркуші «Підсумок» Excel стали зайвими — значення завжди однакові, вибору більше немає. Прибрано
  з обох звітів (лишились «Розгортання» та «СКБД»).

## 2026-07-13 — «Page file» перейменовано на «Диск підкачки»

- В україномовному інтерфейсі та звітах (UI, PDF, Excel) назву колонки/терміна «Page file» замінено
  на «Диск підкачки» — точніша й послідовніша назва рідною мовою. Англійська локалізація лишилась
  без змін («Page file (GB)»).

## 2026-07-13 — ComboBox коректно теміться в темній темі

- `ComboBox` («Тип розгортання», «Database» тощо) у темній темі виглядав як світла заглушка з
  сірим текстом — стоковий WPF-шаблон малює частину хрому системними кольорами й ігнорує прості
  Background/Foreground. Написано повний власний `ControlTemplate` для `ComboBox`/`ComboBoxItem`
  (рамка, стрілка, попап списку) — усе тепер керується `DynamicResource` і коректно виглядає в
  обох темах.

## 2026-07-13 — Обсяг даних БД тепер замінює диск, а не лише піднімає мінімум

- Поле «Обсяг даних БД, ГБ» раніше лише піднімало диск MainData до введеного значення, якщо воно
  БІЛЬШЕ за зашитий у матриці дефолт (300 ГБ) — тобто якщо ввести менше (напр. 150), диск лишався
  незмінним (300 ГБ), і здавалось, що поле не працює. Тепер введене значення **замінює** розмір
  диска MainData напряму (Logs/TempDB — 25% від нього), незалежно від того, більше воно чи менше
  за дефолт із матриці.
- Уточнення по Page file: у чистому Kubernetes-розгортанні колонка «Page file» завжди 0 для
  SQL Server (навмисно — виділений сервер БД не повинен використовувати файл підкачки) і для
  Master/Worker-вузлів (Linux/Ubuntu — концепція Windows page file до них не застосовна). Це не
  баг: файл підкачки рахується лише для Windows-серверів застосунків/веб (App Server/IIS) — там
  він ненульовий і перевірений тестом.

## 2026-07-13 — HR-GraphQL на 1000 сесій, перемикач світлої/темної теми

- **HR-GraphQL** (компонент модуля HR Portal) масштабувався занадто агресивно — 1 репліка на
  кожні 100 користувачів, як у важких подів. Реально HR Portal — самообслуговуючий портал з
  рідкісними легкими сесіями. Додано новий крок формули `Per1000Users`: тепер 1 репліка на кожні
  1000 користувачів HR Portal (напр. 3000 → 3 репліки замість 30). Крос-звʼязок HR→ROBOT/WS
  (формула з еталонного Excel, комірка A40) поки не чіпали — це окреме відкрите питання.
- Додано **перемикач світлої/темної теми** (кнопка в шапці, поруч із мовою) — темна тема на основі
  палітри Catppuccin Mocha з тими самими бренд-кольорами акцентів, що й світла (Catppuccin Latte).
  Перемикається миттєво, без перезапуску застосунку.

## 2026-07-10 — Лише Документообіг, HAProxy без HA, звіти й БД по середовищах

- **Продукт**: прибрано вибір «Стандарт / Документообіг» — застосунок тепер завжди рахує за
  профілем **Документообіг** (Performance). `ProductType`, `StandardModules`, перемикач у UI та
  пов'язана логіка порівняння профілів прибрані.
- **HAProxy**: прибрано режим HA (2 вузли) — балансувальник завжди **1 вузол**.
- **SmartID у гібриді**: прибрано вибір розміщення — SmartID завжди **под у Kubernetes** (окрема
  ВМ на IIS більше не додається).
- **Файл підкачки** тепер враховується як окремий диск у сумі дисків вузла (`DiskPerNodeGb`),
  тож потрапляє у підсумки сховища скрізь (звіт, порівняння середовищ), а не лише в окрему колонку.
- **Компоненти звіту**: у списку компонентів кожного середовища додано галочку «У звіт» —
  дозволяє прибрати компонент із сформованого Excel/PDF, не впливаючи на сам розрахунок.
- **Обсяг даних БД по середовищах**: нове поле «Обсяг даних БД, ГБ» для PROD і кожного похідного
  середовища. Test/PreProd за замовчуванням дорівнюють PROD і не можуть бути менше (можуть бути
  більше); Dev — незалежне значення. Впливає на розмір дисків Data/Logs БД, не на CPU/RAM.
- Побічно виправлено: у гібридному розгортанні похідні середовища (DEV/TEST/PreProd) не
  передавали `Environment` у внутрішні K8s/Windows-конфігурації — редакція СУБД завжди рахувалась
  як PROD. Тепер редакція коректно визначається для кожного середовища.

## 2026-06-30 — Централізований SmartID із вибором розміщення (Kuber/IIS)

- SmartID тепер **один центральний сервіс SSO** на всю систему, розмір — від загальної к-сті
  користувачів. Окремі `SmartID` (Web) / `LMS-SmartID` / `HR-SmartID` прибрано (усуває роздування,
  коли SmartID масштабувався від тисяч облікових записів LMS/HR).
- У **гібриді** додано вибір розміщення SmartID: **Kubernetes** (под) або **веб-сервери (IIS)**
  (без окремої ВМ — IIS і є веб-сервер). У чистому K8s SmartID завжди под, у чистому Windows —
  на веб-серверах IIS. Перемикач показується лише в гібриді.

## 2026-06-30 — Картки середовищ, ForceBPM за розгортанням, версія SQL, назви з еталона

### Налаштування середовищ — картки
- Блок «Додаткові середовища» перероблено: кожне ввімкнене середовище (DEV/TEST/PreProd) —
  окрема **картка** з к-стю користувачів, модулями та опціональними вузлами; вимкнені не показуються.
- Поле к-сті модуля в картці — обрамлене (видно, що редагується), як поле «Користувачів» угорі.

### ForceBPM — керується типом розгортання
- ForceBPM (Kubernetes-only) тепер **без поля к-сті** (масштабується від загальної к-сті) і з
  **заблокованою галочкою**: Kubernetes/Гібрид — увімкнено, Windows — вимкнено (автоматично).
  K8s-сервіси завжди на Ubuntu-вузлах; на чистому Windows ForceBPM немає.

### Версія SQL
- Підпис вузла БД: **MS SQL Server 2025 (мін. 2022)** (рекомендовано 2025, мінімум 2022 — за
  D-AD-ADM-E 2025-10; апаратні вимоги між версіями не відрізняються).

### Назви сервісів — з еталонних розрахунків
- Прибрано самочинні переклади компонентів. Назви беруться дослівно з файлів розрахунків:
  `AS (Сервер додатків)`, `ROBOT (Планувальник)`, `Webrmd (Веб клієнт)`, а решта — канонічні
  технічні (`AS-Local SQL`, `ForceBPM Engine`, `WebAppModeler` …). Вузли — `Master node`, `Worker-node`.

### Звіт
- На вкладці «Підсумок» довга назва (напр. «… Гібрид (K8s + Windows)») більше не обрізається.

## 2026-06-30 — Розділення ВМ по середовищах у звіті, HA для HAProxy, pagefile БД

### Звіт Excel — аркуш «ВМ по середовищах»
- Замість однієї суцільної таблиці кожне середовище (PROD/PreProd/TEST/DEV) тепер **окремий
  блок-таблиця**: синій підпис із кількістю користувачів → шапка → рядки ВМ → підсумок «Разом» →
  рамка + відступ між блоками. Стиль узгоджено з аркушем «Інфраструктура».
- Колонку «Середовище» прибрано з рядків (вона у підписі блоку) — таблиця компактніша.

### HAProxy — відмовостійкість (High Availability)
- Додано опцію **«HAProxy HA (2 вузли)»**: балансувальник більше не мусить бути єдиною точкою
  відмови. У режимі HA додаються **2 вузли active/passive** (keepalived/VRRP, спільний VIP)
  замість одного, з відповідною приміткою у вузлі.
- Для PROD — чекбокс біля HAProxy (активний лише коли HAProxy увімкнено); для DEV/TEST/PreProd —
  окремий рядок «HAProxy HA (2 вузли)» у сітці «Додаткові вузли по середовищах» (керується незалежно).
- Залізо HAProxy лишається 2 ядра / 4 ГБ — для підтримуваних обсягів цього достатньо; масштабується
  лише кількість вузлів, а не специфікація.

### Файл підкачки (page file) сервера БД
- Прибрано фолбек, що нав'язував вузлу БД на Windows/Hybrid pagefile, рівний обсягу RAM. Виділений
  SQL Server не потребує pagefile, масштабованого від RAM (документ D-AD-ADM-E / еталонний калькулятор
  задають pagefile лише для серверів додатків/веб). Тепер усі режими (K8s/Windows/Hybrid) узгоджені —
  для вузла БД pagefile не показується.

## 2026-06-24 — Зрозуміліший аркуш «Підсумок» у звіті

- Профіль навантаження більше не показується англійським «Basic/Performance» — тепер
  «Базовий (звичайне навантаження)» / «Продуктивний (підвищене навантаження)».
- Аркуш «Підсумок» (Excel) адаптовано: змістовна назва замість «Project», пояснювальний вступ,
  секції «Параметри» / «Підсумкові потреби» / «Контейнери», додано «Продукт», зрозуміліші підписи.
- У HTML-вступ додано продукт і профіль українською.

## 2026-06-24 — Колонки таблиць розкриті за замовчуванням

- Виправлено згортання колонок у **всіх** таблицях вкладки результатів (інфраструктура, середовища,
  розбивка ВМ, звірка): колонки з `Width="*"` згорталися через горизонтальну прокрутку сторінки,
  яка давала нескінченну ширину. Тепер горизонтальну прокрутку сторінки вимкнено, текстові колонки
  авто-ширини за вмістом — без потреби клікати-розтягувати.

## 2026-06-24 — Повний екран, скрол колесом будь-де, коректні користувачі середовищ

- Вікно відкривається **на повний екран** (Maximized) із мінімальними розмірами.
- **Прокрутка колесом миші** на вкладці результатів працює будь-де, а не лише по смужці
  (раніше таблиці «з'їдали» прокрутку).
- **Середовища**: TEST/PreProd показують **масштабоване число користувачів** (узгоджено з
  урізаною потужністю), а не повні 100. Колонку перейменовано на «Користувачів» (UI/HTML/Excel).

## 2026-06-24 — Зрозуміліший звіт, підказки в UI, довгоживучий підпис

- **Звіт** перероблено для не-ІТ читачів: пояснювальний вступ, підписи під показниками,
  колонка «Призначення» серверів (що робить кожна ВМ), глосарій термінів (CPU/RAM/IOPS/диски…),
  пояснення середовищ (PROD/DEV/TEST/PreProd) та резерву під бекап — у HTML та Excel.
- **Підказки (tooltips)** додано на всі поля калькулятора (користувачі, продукт, розгортання,
  СУБД, обсяг даних БД, середовища) та на показники результатів.
- `sign.ps1`: параметр `-Years` (типово 25 — практично «без терміну») та `-ExportCert`
  (експорт публічного `.cer` для роздачі на всі ПК через GPO).

## 2026-06-24 — Кількість користувачів на модуль

- Кожен модуль (LMS, HR Portal, ForceBPM …) може мати **власну кількість користувачів** —
  поле праворуч від чекбокса модуля. Порожньо = загальна кількість користувачів проєкту;
  понад загальну не піднімається (для DEV природно обмежується кількістю ліцензій DEV).
- Репліки компонентів модуля рахуються за його кількістю користувачів, а не за загальною.

## 2026-06-24 — Бекап від обсягу даних БД, версія/редакція СУБД, IOPS по вузлах, звірка з документом

### Бекап і диски БД
- Резерв під бекап тепер рахується від **обсягу реляційних даних БД** (нове поле «Обсяг даних БД, ГБ»),
  а не від усього виділеного диска. Усунено роздуття (раніше DEV/TEST сягали 4 ТБ під базу в 10 ГБ).
- Бекап-резерв додається до **кожного** середовища, включно з PROD (раніше — лише DEV/TEST).
- У Windows-розгортанні примітка зазначає ширший склад бекапу (БД + веб/клієнт/мережева), у Linux — лише БД.
- Диски `Data`/`Logs+TempDB` вузла БД масштабуються за обсягом даних (з розумними мінімумами) —
  немає терабайтних дисків під малу базу.

### СУБД
- У таблиці інфраструктури та звітах виводиться **версія/редакція СУБД** (MS SQL Server 2022,
  PostgreSQL 17+, Oracle 19c). Мінімальна версія SQL — 2022.
- Якщо рекомендований обсяг ОЗП SQL **> 128 ГБ** → автоматично редакція **Enterprise** (ліміт Standard),
  з відповідною приміткою.

### IOPS, ВМ, звіт
- «Всього IOPS» більше не сумується між дисками/вузлами — показник = IOPS **вузла БД** (визначальний),
  а IOPS кожного вузла лишаються в таблиці по вузлах.
- KPI/заголовки «Вузли» → «ВМ (серверів)».
- На вкладці результатів додано **прокрутку** (ScrollViewer).
- Розбивка **ВМ для кожного середовища** (PROD/DEV/TEST/PreProd) — в UI, HTML та Excel.
- Додано **звірку розрахунку з вимогами документа** D-AD-ADM-E (сервер БД: CPU/RAM/IOPS/затримка)
  з підсвічуванням відхилень — в UI, HTML, Excel, XML.
- Назву звіту узгоджено зі складом (користувачі + тип розгортання).
- 111 тестів, усі проходять.

## 2026-06-18 — Чистота коду, тести ViewModel, скрипт підпису

- Прибрано мертвий `MergeFromJson` із `LocalizationService` (зовнішні JSON-локалізації видалені раніше).
- `ResultsPresenter` отримує залежності (`ConfigExportService`, `IValidationEngine`) через
  конструктор (DI) замість `new` — узгоджено з рештою сервісів; зареєстровано в `App`.
- Додано `MainViewModelTests` — перевірка команд і прив'язок UI на рівні ViewModel
  (розрахунок, історія, перемикання розгортання/продукту/мови, запит подів, некоректний ввід).
- Додано [`sign.ps1`](sign.ps1) — підпис `publish\*.exe` (самопідписаний або CA-сертифікат через `-PfxPath`).
- 84 тести, усі проходять.

## 2026-06-18 — Узгодження «Всього vCPU/RAM» + документування формул реплік

### «Всього vCPU/RAM» — єдина основа для всіх режимів
- Підсумкові `TotalCpu` / `TotalRamGb` тепер означають **фізичні ресурси всіх вузлів**
  (`Σ вузол.Cpu × кількість`) однаково для K8s, Windows і Hybrid.
- Раніше K8s показував суму **запитів подів** (напр. 25.85 на 100 користувачів), що занижувало
  фізичний слід і було несумісним з Windows (фізичні ядра ВМ). Тепер K8s 100 користувачів →
  44 vCPU (SQL 8 + Master 4 + Worker 8×4), зіставно з Windows (20).
- Запит подів і далі використовується внутрішньо для розрахунку кількості worker-вузлів.
- Hybrid: підсумок рахується з фінального списку вузлів (після дедуплікації спільної БД) —
  усунено подвійний облік SQL-вузла.
- Для наочності додано окрему величину **«запит подів K8s»** (`PodCpu`/`PodRamGb`) — показується
  на вкладці результатів та у звітах TXT/HTML поряд із фізичним підсумком, щоб різниця між
  «скільки просять поди» і «скільки заліза провіжинимо» була зрозумілою. Для Windows = 0.

### Формули реплік — свідома калібровка (не змінено)
- Поди `ROBOT`, `WS (Веб сервіси)`, `ForceBPM Engine`, `HR Portal (SmartID/GraphQL)` масштабуються
  за каліброваними формулами, а не за загальним правилом «крок 25». Це навмисне інженерне рішення:
  фонові служби й рушії не масштабуються лінійно з користувачами (інакше — десятки зайвих реплік),
  а «фікс=1» недостатньо на великих обсягах. Більшість подів масштабуються рівно «крок 25» (за Excel).

## 2026-06-18 — Звірка з Excel та видалення хмарного коду

### Звірка розрахунків з Excel (Калькулятор.xlsx)
- Підтверджено, що сайзинг СУБД (MSSQL Стандарт/Продуктивний), діапазони App/Web-серверів,
  вузли інфраструктури, 4-дискова розкладка SQL та Windows-підсумок дзеркалять таблицю
  (напр. Windows 100 користувачів → 20 vCPU / 104 GB / 1200 IOPS).
- Зафіксовано, що формули реплік для `ROBOT`, `WS (Веб сервіси)`, `ForceBPM Engine` та `HR Portal`
  не виводяться напряму з правила «крок 25» — це калібровані формули (рішення про модель — за власником продукту).

### Видалено невикористаний хмарний/IaC-код
- Прибрано генератори IaC (Terraform, ARM, Bicep, Pulumi, Ansible, Helm, маніфести K8s, GCP/AWS),
  `CostEstimator`, `DiagramBuilder` (SVG), експорт SVG/Mermaid та мапінг розмірів Azure-ВМ — вони
  не були виведені в інтерфейс.
- Звіти TXT/HTML очищено від хмарних згадок (Azure, VM Size, «Cloud Report») — лише ресурси.
- Видалено відповідні тести; залишилось 72 тести, усі проходять.

## 2026-06-18 — Аудит: коректність, безпека, чистка репозиторію

### Виправлено
- **Підрахунок дисків.** Сумарний обсяг сховища (`TotalStorageGb`) тепер враховує розділені диски
  вузла (OS / Logs+TempDB / Data / Content), а не лише перший диск. Виправлено в рушії сайзингу,
  підсумку результатів та оцінці вартості.
- **XSS в експорті.** Назви вузлів і компонентів (походять з імпортованого Excel) тепер екрануються
  в HTML- та SVG-звітах.
- **Невалідний Terraform.** Генератор тепер створює `azurerm_subnet` та `azurerm_network_interface`
  замість посилання на неіснуючий ресурс; додано `source_image_reference`.
- **Ідентифікатори IaC.** Введено єдиний хелпер `SanitizeResourceName` — некоректні символи у назвах
  вузлів (пробіли, дужки, слеші) більше не потрапляють в ідентифікатори Terraform/ARM/Bicep/Pulumi.
- **Секрети в K8s-маніфестах.** Креденшали виносяться в `Secret` і підключаються через `secretKeyRef`,
  а не зберігаються у відкритому `env.value`.
- **Глобальний обробник винятків.** Непередбачені помилки більше не аварійно завершують застосунок.
- **Аліасинг матриці.** `CopyMatrix` робить глибоке копіювання — редагування грідів не мутує спільний стан.
- **Дрібниці.** Обмеження кількості користувачів (1–5000); тип СУБД враховується у профілі порівняння;
  усунено повторні підписки на події у вкладці калькулятора; прибрано дублювання логіки реплік та виклику `GetConfig`.

### Інфраструктура
- Прибрано з git бінарні артефакти збірки (`publish/*.exe`, `publish/*.pdb`) та згенеровані приклади звітів.
- Документацію уніфіковано та переведено повністю українською (`README.md`, цей файл).
- Додано регресійні тести (107 загалом, усі проходять).

## Раніше

- **DI-контейнер** — Microsoft.Extensions.DependencyInjection, налаштування в `App.xaml.cs`.
- **Поділ MainViewModel** — виділено `MatrixViewModel`.
- **Підтримка СУБД** — додано PostgreSQL та Oracle поряд з MS SQL Server.
- **Імпорт Excel** — header-agnostic парсер матриці сайзингу.
- **Локалізація** — українська та англійська.
- **CI/CD** — GitHub Actions (build + test) у `.github/workflows/ci.yml`.
- **Видалено AI-функціонал** — застосунок працює лише на rule-based розрахунках.
