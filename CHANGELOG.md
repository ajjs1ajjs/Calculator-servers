# Історія змін

## 2026-06-24 — Колонки таблиць розкриті за замовчуванням

- Виправлено згортання колонок у **всіх** таблицях вкладки результатів (інфраструктура, середовища,
  розбивка ВМ, звірка): колонки з `Width="*"` згорталися через горизонтальну прокрутку сторінки,
  яка давала нескінченну ширину. Тепер горизонтальну прокрутку сторінки вимкнено, текстові колонки
  авто-ширини за вмістом — без потреби клікати-розтягувати.

## 2026-06-24 — Повний екран, скрол колесом будь-де, коректні користувачі середовищ

- Вікно відкривається **на повний екран** (Maximized) із мінімальними розмірами.
- **Прокрутка колесом миші** на вкладці результатів працює будь-де, а не лише по смужці
  (раніше таблиці «з'їдали» прокрутку).
- **Середовища**: TEST/PreProd показують **масштабоване число користувачів** (узгоджено з
  урізаною потужністю), а не повні 100. Колонку перейменовано на «Користувачів» (UI/HTML/Excel).

## 2026-06-24 — Зрозуміліший звіт, підказки в UI, довгоживучий підпис

- **Звіт** перероблено для не-ІТ читачів: пояснювальний вступ, підписи під показниками,
  колонка «Призначення» серверів (що робить кожна ВМ), глосарій термінів (CPU/RAM/IOPS/диски…),
  пояснення середовищ (PROD/DEV/TEST/PreProd) та резерву під бекап — у HTML та Excel.
- **Підказки (tooltips)** додано на всі поля калькулятора (користувачі, продукт, розгортання,
  СУБД, обсяг даних БД, середовища) та на показники результатів.
- `sign.ps1`: параметр `-Years` (типово 25 — практично «без терміну») та `-ExportCert`
  (експорт публічного `.cer` для роздачі на всі ПК через GPO).

## 2026-06-24 — Кількість користувачів на модуль

- Кожен модуль (LMS, HR Portal, ForceBPM …) може мати **власну кількість користувачів** —
  поле праворуч від чекбокса модуля. Порожньо = загальна кількість користувачів проєкту;
  понад загальну не піднімається (для DEV природно обмежується кількістю ліцензій DEV).
- Репліки компонентів модуля рахуються за його кількістю користувачів, а не за загальною.

## 2026-06-24 — Бекап від обсягу даних БД, версія/редакція СУБД, IOPS по вузлах, звірка з документом

### Бекап і диски БД
- Резерв під бекап тепер рахується від **обсягу реляційних даних БД** (нове поле «Обсяг даних БД, ГБ»),
  а не від усього виділеного диска. Усунено роздуття (раніше DEV/TEST сягали 4 ТБ під базу в 10 ГБ).
- Бекап-резерв додається до **кожного** середовища, включно з PROD (раніше — лише DEV/TEST).
- У Windows-розгортанні примітка зазначає ширший склад бекапу (БД + веб/клієнт/мережева), у Linux — лише БД.
- Диски `Data`/`Logs+TempDB` вузла БД масштабуються за обсягом даних (з розумними мінімумами) —
  немає терабайтних дисків під малу базу.

### СУБД
- У таблиці інфраструктури та звітах виводиться **версія/редакція СУБД** (MS SQL Server 2022,
  PostgreSQL 17+, Oracle 19c). Мінімальна версія SQL — 2022.
- Якщо рекомендований обсяг ОЗП SQL **> 128 ГБ** → автоматично редакція **Enterprise** (ліміт Standard),
  з відповідною приміткою.

### IOPS, ВМ, звіт
- «Всього IOPS» більше не сумується між дисками/вузлами — показник = IOPS **вузла БД** (визначальний),
  а IOPS кожного вузла лишаються в таблиці по вузлах.
- KPI/заголовки «Вузли» → «ВМ (серверів)».
- На вкладці результатів додано **прокрутку** (ScrollViewer).
- Розбивка **ВМ для кожного середовища** (PROD/DEV/TEST/PreProd) — в UI, HTML та Excel.
- Додано **звірку розрахунку з вимогами документа** D-AD-ADM-E (сервер БД: CPU/RAM/IOPS/затримка)
  з підсвічуванням відхилень — в UI, HTML, Excel, XML.
- Назву звіту узгоджено зі складом (користувачі + тип розгортання).
- 111 тестів, усі проходять.

## 2026-06-18 — Чистота коду, тести ViewModel, скрипт підпису

- Прибрано мертвий `MergeFromJson` із `LocalizationService` (зовнішні JSON-локалізації видалені раніше).
- `ResultsPresenter` отримує залежності (`ConfigExportService`, `IValidationEngine`) через
  конструктор (DI) замість `new` — узгоджено з рештою сервісів; зареєстровано в `App`.
- Додано `MainViewModelTests` — перевірка команд і прив'язок UI на рівні ViewModel
  (розрахунок, історія, перемикання розгортання/продукту/мови, запит подів, некоректний ввід).
- Додано [`sign.ps1`](sign.ps1) — підпис `publish\*.exe` (самопідписаний або CA-сертифікат через `-PfxPath`).
- 84 тести, усі проходять.

## 2026-06-18 — Узгодження «Всього vCPU/RAM» + документування формул реплік

### «Всього vCPU/RAM» — єдина основа для всіх режимів
- Підсумкові `TotalCpu` / `TotalRamGb` тепер означають **фізичні ресурси всіх вузлів**
  (`Σ вузол.Cpu × кількість`) однаково для K8s, Windows і Hybrid.
- Раніше K8s показував суму **запитів подів** (напр. 25.85 на 100 користувачів), що занижувало
  фізичний слід і було несумісним з Windows (фізичні ядра ВМ). Тепер K8s 100 користувачів →
  44 vCPU (SQL 8 + Master 4 + Worker 8×4), зіставно з Windows (20).
- Запит подів і далі використовується внутрішньо для розрахунку кількості worker-вузлів.
- Hybrid: підсумок рахується з фінального списку вузлів (після дедуплікації спільної БД) —
  усунено подвійний облік SQL-вузла.
- Для наочності додано окрему величину **«запит подів K8s»** (`PodCpu`/`PodRamGb`) — показується
  на вкладці результатів та у звітах TXT/HTML поряд із фізичним підсумком, щоб різниця між
  «скільки просять поди» і «скільки заліза провіжинимо» була зрозумілою. Для Windows = 0.

### Формули реплік — свідома калібровка (не змінено)
- Поди `ROBOT`, `WS (Веб сервіси)`, `ForceBPM Engine`, `HR Portal (SmartID/GraphQL)` масштабуються
  за каліброваними формулами, а не за загальним правилом «крок 25». Це навмисне інженерне рішення:
  фонові служби й рушії не масштабуються лінійно з користувачами (інакше — десятки зайвих реплік),
  а «фікс=1» недостатньо на великих обсягах. Більшість подів масштабуються рівно «крок 25» (за Excel).

## 2026-06-18 — Звірка з Excel та видалення хмарного коду

### Звірка розрахунків з Excel (Калькулятор.xlsx)
- Підтверджено, що сайзинг СУБД (MSSQL Стандарт/Продуктивний), діапазони App/Web-серверів,
  вузли інфраструктури, 4-дискова розкладка SQL та Windows-підсумок дзеркалять таблицю
  (напр. Windows 100 користувачів → 20 vCPU / 104 GB / 1200 IOPS).
- Зафіксовано, що формули реплік для `ROBOT`, `WS (Веб сервіси)`, `ForceBPM Engine` та `HR Portal`
  не виводяться напряму з правила «крок 25» — це калібровані формули (рішення про модель — за власником продукту).

### Видалено невикористаний хмарний/IaC-код
- Прибрано генератори IaC (Terraform, ARM, Bicep, Pulumi, Ansible, Helm, маніфести K8s, GCP/AWS),
  `CostEstimator`, `DiagramBuilder` (SVG), експорт SVG/Mermaid та мапінг розмірів Azure-ВМ — вони
  не були виведені в інтерфейс.
- Звіти TXT/HTML очищено від хмарних згадок (Azure, VM Size, «Cloud Report») — лише ресурси.
- Видалено відповідні тести; залишилось 72 тести, усі проходять.

## 2026-06-18 — Аудит: коректність, безпека, чистка репозиторію

### Виправлено
- **Підрахунок дисків.** Сумарний обсяг сховища (`TotalStorageGb`) тепер враховує розділені диски
  вузла (OS / Logs+TempDB / Data / Content), а не лише перший диск. Виправлено в рушії сайзингу,
  підсумку результатів та оцінці вартості.
- **XSS в експорті.** Назви вузлів і компонентів (походять з імпортованого Excel) тепер екрануються
  в HTML- та SVG-звітах.
- **Невалідний Terraform.** Генератор тепер створює `azurerm_subnet` та `azurerm_network_interface`
  замість посилання на неіснуючий ресурс; додано `source_image_reference`.
- **Ідентифікатори IaC.** Введено єдиний хелпер `SanitizeResourceName` — некоректні символи у назвах
  вузлів (пробіли, дужки, слеші) більше не потрапляють в ідентифікатори Terraform/ARM/Bicep/Pulumi.
- **Секрети в K8s-маніфестах.** Креденшали виносяться в `Secret` і підключаються через `secretKeyRef`,
  а не зберігаються у відкритому `env.value`.
- **Глобальний обробник винятків.** Непередбачені помилки більше не аварійно завершують застосунок.
- **Аліасинг матриці.** `CopyMatrix` робить глибоке копіювання — редагування грідів не мутує спільний стан.
- **Дрібниці.** Обмеження кількості користувачів (1–5000); тип СУБД враховується у профілі порівняння;
  усунено повторні підписки на події у вкладці калькулятора; прибрано дублювання логіки реплік та виклику `GetConfig`.

### Інфраструктура
- Прибрано з git бінарні артефакти збірки (`publish/*.exe`, `publish/*.pdb`) та згенеровані приклади звітів.
- Документацію уніфіковано та переведено повністю українською (`README.md`, цей файл).
- Додано регресійні тести (107 загалом, усі проходять).

## Раніше

- **DI-контейнер** — Microsoft.Extensions.DependencyInjection, налаштування в `App.xaml.cs`.
- **Поділ MainViewModel** — виділено `MatrixViewModel`.
- **Підтримка СУБД** — додано PostgreSQL та Oracle поряд з MS SQL Server.
- **Імпорт Excel** — header-agnostic парсер матриці сайзингу.
- **Локалізація** — українська та англійська.
- **CI/CD** — GitHub Actions (build + test) у `.github/workflows/ci.yml`.
- **Видалено AI-функціонал** — застосунок працює лише на rule-based розрахунках.
