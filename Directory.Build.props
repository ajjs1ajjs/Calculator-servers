<Project>
  <PropertyGroup>
    <!-- Єдине джерело версії застосунку — використовується AIResourceCalculator.csproj
         (Version/FileVersion/InformationalVersion, звідти ж читає UpdateCheckService)
         та AIResourceCalculator.Installer.wixproj (ProductVersion MSI).
         Бампати тут при кожному релізі — release.ps1 звіряє це значення з тегами git
         і не дасть опублікувати реліз без бампу. -->
    <AppVersion>1.1.2</AppVersion>
  </PropertyGroup>
</Project>
