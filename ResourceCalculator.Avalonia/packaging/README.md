# Пакування

Ця тека містить шаблони для пакування Avalonia-версії (Windows).

## Іконки

- Windows: `ResourceCalculator/icon.ico` (256x256) — використовується і для Avalonia win-версії.

`release.ps1` пакує Avalonia win-x64 в `ITE.ResourceCalculator-avalonia-win-x64.zip`.